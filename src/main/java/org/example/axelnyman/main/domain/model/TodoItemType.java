package org.example.axelnyman.main.domain.model;

/**
 * Enum representing the type of todo item.
 * PAYMENT: Manual expense payment that needs to be made
 * TRANSFER: Money transfer between bank accounts
 */
public enum TodoItemType {
    PAYMENT,
    TRANSFER
}
